{% assign loading = include.loading|default:"eager" %}
{% if include.caption %}
    {% if include.type %}
        {% cloudinary 
            {{ include.type }} 
            {{ include.path }}
            alt="{{ include.alt | escape }}"
            caption="{{ include.caption | markdownify }}"
            loading="{{ loading }}"
        %}
    {% else %}
        {% cloudinary 
            {{ include.path }}
            alt="{{ include.alt | escape }}"
            caption="{{ include.caption | markdownify }}"
            loading="{{ loading }}"
        %}
    {% endif %}
{% else %}
    {% if include.type %}
        {% cloudinary 
            {{ include.type }} 
            {{ include.path }}
            alt="{{ include.alt | escape }}"
            loading="{{ loading }}"
        %}
    {% else %}
        {% cloudinary 
            {{ include.path }}
            alt="{{ include.alt | escape }}"
            loading="{{ loading }}"
        %}
    {% endif %}
{% endif %}